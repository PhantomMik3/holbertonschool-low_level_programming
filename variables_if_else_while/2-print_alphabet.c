#include <stdio.h>

/**
 * main - print alphabet in lowercase
 * Return: Always (0) (Successful)
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
