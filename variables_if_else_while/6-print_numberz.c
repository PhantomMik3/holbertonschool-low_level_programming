#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *  main - main, int, d, for, return
 *  Return: Always (0) (Succesfull)
 */

int main(void)

{
	int d;

	for (d = '0'; d <= '9'; d++)
	putchar(d);
	putchar('\n');

	return (0);
}
